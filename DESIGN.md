# 청모 — 청첩장 모임 날짜 투표 웹앱 · 설계 문서

> 결혼 앞두고 청첩장 밥약을 그룹별로 잡되, 신랑/신부 일정이 겹치지 않게 자동으로 막아주는 도구.

작성일: 2026-07-29 · 모드: office-hours (Builder)

---

## 1. 한 줄 정의

"청첩장 돌릴 밥약, 그룹별로 날짜 투표하고 신랑·신부 일정은 자동으로 안 겹치게."

핵심 차별점 = **확정된 슬롯이 다른 모든 그룹 투표에서 잠긴다** (When2meet·언제어때엔 없는 부분).

## 2. 확정된 결정

| 항목 | 결정 |
|---|---|
| 슬롯 단위 | 그룹마다 호스트가 **점심 / 저녁 / 둘다** 지정. "둘다"면 멤버가 가능한 쪽 선택 |
| 날짜 확정 | **호스트 수동 확정** (집계 보고 직접 결정) |
| 호스팅 | **로컬 Docker 자가호스팅** (클라우드 결제 없음) |
| 외부 접속 | **Cloudflare Tunnel (무료)** — 공개 https 주소 제공 |
| 호스트 머신 | **Windows 데스크톱**(항상 켜둠). 개발은 맥북에서 |
| 투표자 진입 | 호스트가 **이름 등록 → 개인 링크 공유** → 접속 시 "안녕하세요 ○○님" 개인화 환영 (무로그인) |

## 3. 아키텍처 & 호스팅

```
[친구 폰] --https--> [Cloudflare Tunnel] --> [Windows 데스크톱]
                                              └─ docker compose
                                                 ├─ web  (Next.js)
                                                 └─ db   (Postgres)
```

- **스택**: Next.js(App Router, 서버 액션) + Postgres. Prisma로 스키마 관리.
- **왜 풀스택**: "확정 시 전역 잠금"은 공유 DB + 트랜잭션이 있어야 성립. 프론트 only로는 불가.
- **배포**: `docker compose up` 한 방. web + db 두 컨테이너.
- **공개 주소**: `cloudflared tunnel` (무료, 계정만 있으면 됨). 결제 리소스 0.
- **운영 팁**: 이벤트가 끝나면(결혼식 후) 컨테이너 내리면 끝. 상시 서비스 아님 → 자가호스팅에 딱 맞음.

## 4. 데이터 모델 (핵심)

```
Event        신랑·신부 이름, 결혼식 날짜, 후보 기간(start~end), 자동차단일(예식일+버퍼)
Group        name, slot_type(LUNCH|DINNER|BOTH), 초대 문구, 확정여부
Member       name, token(개인 링크용 고유값), group_id
Vote         member_id, date, slot(LUNCH|DINNER), status(O|MAYBE|X)
Confirmation group_id, date, slot
```

**중복 방지의 핵심 = DB 유니크 제약:**

```
UNIQUE (event_id, date, slot)  ON Confirmation
```

- 한 (날짜, 점심/저녁) 슬롯은 이벤트 전체에서 **딱 한 번만** 확정 가능.
- 그룹 A가 10/12 저녁 확정 → 다른 그룹은 10/12 저녁 확정 불가(DB가 거부).
- 같은 날 **점심은 여전히 열림** → 다른 그룹이 10/12 점심 잡을 수 있음.
- 동시에 두 그룹이 같은 슬롯을 확정 시도 → 트랜잭션 + 유니크 제약으로 한쪽만 성공(경쟁 조건 방지).

## 5. 핵심 사용자 흐름

### 호스트 (신랑/신부)
1. 이벤트 생성 (이름, 결혼식 날짜, 후보 기간) → 예식일·버퍼일 자동 차단
2. 그룹 생성 + 그룹별 슬롯타입(점심/저녁/둘다) + 초대 문구
3. 멤버 이름 등록 → 개인 링크 자동 생성 → 카톡으로 공유
4. 대시보드에서 그룹별 집계·히트맵 확인
5. **확정** 클릭 → 슬롯 전역 잠금 → 확정 카톡 문구 자동 생성

### 투표자 (그룹 멤버)
1. 개인 링크 접속 → "안녕하세요 ○○님 🎉" 개인화 환영 + 초대 문구
2. 후보 날짜에 O / △ / X 원탭 (둘다 그룹이면 점심/저녁 각각)
3. 제출 → "고마워요! 확정되면 알려줄게요"
4. (재접속) 자기 표 수정 가능. 이미 다른 그룹이 잡은 슬롯은 회색 + "이미 잡힌 날이에요"

## 6. 화면 목록

**호스트**: 이벤트 설정 · 그룹 관리 · 멤버/링크 관리 · 통합 대시보드(진행률·남은 슬롯·중복방지 로그) · 그룹별 집계·확정
**투표자**: 개인화 환영 + 투표 · 제출 완료 · (확정 후) 모임 안내

## 7. 디자인 톤 — "정성 있어 보이게"

- 웨딩 무드 팔레트(크림/블러시/세이지), 세리프 디스플레이 폰트, 은은한 플로럴 모티프
- 상단에 두 사람 이름 + 결혼식 날짜
- 그룹별 개인화 초대 문구, 접속 시 이름 환영
- 모바일 원탭 O/△/X (링크는 대부분 카톡→폰에서 열림)
- 날짜별 가능 인원 히트맵
- 확정 순간 컨페티 + 카톡 공유 메시지 자동 생성

## 8. 범위

**MVP**
- 이벤트/후보기간 생성, 예식일·버퍼 자동 차단
- 그룹 생성 + 슬롯타입 지정 + 초대 문구
- 멤버 이름 등록 + 개인 링크
- 개인화 환영 + O/△/X 투표 + 집계·히트맵
- 호스트 수동 확정 + **전역 슬롯 잠금(유니크 제약)**
- Docker Compose + Cloudflare Tunnel 배포

**v2 (나중)**
- 확정 카톡 문구 자동 생성, 장소/인원 메모
- 통합 대시보드 남은 슬롯 경고("주말 저녁 부족")
- 확정 컨페티 애니메이션, 리마인더
- 확정 취소 → 슬롯 반환

## 9. 마일스톤 (제안)

1. **뼈대**: Next.js + Postgres + Prisma 스키마, docker-compose
2. **호스트 CRUD**: 이벤트·그룹·멤버·개인링크 생성
3. **투표**: 개인화 환영 + O/△/X + 집계
4. **확정 & 잠금**: 유니크 제약 + 전역 회색처리
5. **디자인 입히기**: 웨딩 무드 + 모바일
6. **배포**: docker + cloudflared, 폰으로 실접속 테스트

## 10. 다음 액션 (숙제)

1. **Cloudflare 무료 계정 만들고 도메인/Tunnel 준비** (또는 우선 ngrok 무료로 접속 테스트)
2. **실제 그룹·인원 목록 적어보기** — 그룹 몇 개, 각 몇 명, 점심/저녁 중 뭐? (이 숫자가 "슬롯 부족" 여부를 결정)
3. Windows 데스크톱을 상시 호스트로 쓸지 확정
```
